[project]
name = "nepalidates"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ashish Kandu", email = "ashishkandu43@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "nepali-datetime>=1.0.8.4",
    "pybsdate>=0.3.2",
]

[project.scripts]
nepalidates = "nepalidates:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
